# دليل النشر الكامل — Ahmed Visual

من الصفر حتى الموقع الحي على دومينك `ahmedvis.com`.

هذا الموقع مبني بثلاث طبقات احترافية:

| الطبقة | الأداة | الوظيفة |
|---|---|---|
| الكود | **GitHub** | يخزّن كل ملفات الموقع |
| الاستضافة | **Netlify** | ينشر الموقع من GitHub تلقائيًا، ويربط دومين `ahmedvis.com` |
| المحتوى والصور | **Supabase** | قاعدة بيانات + مساحة تخزين، تشغّل لوحة التحكم فعليًا (تسجيل دخول، حفظ فوري، رفع صور) |

النتيجة النهائية: تفتح `ahmedvis.com/admin.html`، تسجّل دخول، تعدّل أي شيء (نصوص، صور، مشاريع) — والتعديل يظهر **فورًا** على الموقع الحي لأي زائر، من غير ما تلمس GitHub أو Netlify بعد اليوم الأول.

**ترتيب التنفيذ المهم:** Supabase أولاً (الجزء الأول) لأن الموقع ما يشتغل بدونه، ثم GitHub، ثم Netlify.

---

## الجزء الأول: إعداد Supabase (قاعدة البيانات)

### 1. أنشئ مشروع Supabase

1. روح إلى [supabase.com](https://supabase.com) وسجّل دخول (أو أنشئ حساب مجاني).
2. اضغط **New Project**.
3. اسم المشروع: `ahmed-visual` (أو أي اسم تحبه)، وكلمة مرور قوية لقاعدة البيانات — احفظها في مكان آمن.
4. اختر المنطقة (Region) الأقرب لجمهورك.
5. اضغط **Create new project** وانتظر دقيقة أو دقيقتين حتى يجهز.

### 2. شغّل ملف قاعدة البيانات

1. من القائمة الجانبية، افتح **SQL Editor**.
2. اضغط **New query**.
3. افتح ملف `supabase-schema.sql` (في هذا المجلد)، وانسخ **كل** محتواه.
4. الصقه في محرر SQL، ثم اضغط **Run**.
5. تأكد إنه ما طلع أي خطأ أحمر — "Success" يعني كل الجداول والصلاحيات جاهزة.

### 3. فعّل تسجيل الدخول بالبريد وكلمة المرور

1. من القائمة الجانبية: **Authentication → Providers**، وتأكد إن **Email** مفعّل.
2. روح إلى **Authentication → Users** واضغط **Add user → Create new user**.
3. أدخل بريدك الإلكتروني وكلمة مرور قوية — هذا حساب دخولك للوحة التحكم.
4. فعّل **Auto Confirm User** (أو أكّد البريد يدويًا من نفس الصفحة).

> هذا حسابك الشخصي فقط كصاحب الموقع. لا يوجد تسجيل عام مفتوح، فلا أحد غيرك يقدر يدخل اللوحة.

### 4. انسخ مفاتيح الاتصال

1. روح إلى **Project Settings** (⚙️) **→ API**.
2. انسخ القيمتين: **Project URL** و **anon public** key.
3. افتح ملف `supabase-config.js` في هذا المشروع، واستبدل:
   ```js
   const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';
   const SUPABASE_ANON_KEY = 'YOUR-ANON-PUBLIC-KEY';
   ```
   بالقيم الحقيقية. احفظ الملف.

> هذا المفتاح (anon key) آمن يكون ظاهر في كود الموقع — هذا استخدامه الطبيعي، والحماية الفعلية موجودة في قواعد RLS التي فعّلناها في الخطوة 2 (لا أحد يعدّل البيانات إلا بعد تسجيل دخول صحيح).

---

## الجزء الثاني: رفع الكود على GitHub

### 1. أنشئ حساب GitHub (إذا ما عندك)

روح إلى [github.com](https://github.com) وسجّل دخول بحسابك الموجود.

### 2. أنشئ مستودع (Repository) جديد

1. اضغط الزر **+** أعلى يمين الصفحة → **New repository**.
2. **Repository name:** اكتب `ahmed-visual-site` (أو أي اسم تفضله).
3. اترك **Description** فاضي أو اكتب وصف بسيط.
4. اختر **Private** (موصى به، لأن المستودع يحتوي إعدادات مشروعك) — أو Public إذا تفضل.
5. **لا** تفعّل "Add a README file" ولا "Add .gitignore" ولا "Choose a license" — الملفات هذي موجودة بالفعل في المشروع.
6. اضغط **Create repository**.

بعد الإنشاء، بتوصل لصفحة فاضية فيها تعليمات — تجاهلها وروح للخطوة التالية.

### 3. ارفع ملفات الموقع

**الطريقة الأسهل (بدون أي أوامر — عبر المتصفح مباشرة):**

1. في صفحة المستودع الفارغ، اضغط الرابط الأزرق **uploading an existing file**.
2. فكّ ضغط الملف المرفق من عندي (`ahmed-visual-site.zip`) على جهازك أولاً.
3. افتح مجلد الموقع، وحدّد **كل** الملفات (`index.html`, `admin.html`, `style.css`, `admin.js`... إلخ) واسحبها بالكامل وأفلتها في صفحة GitHub.

   > ملاحظة: اسحب محتوى المجلد (الملفات نفسها) لا المجلد ذاته، حتى تكون الملفات في جذر المستودع مباشرة.

4. انزل تحت، اكتب رسالة commit بسيطة مثل `Initial site`، واضغط **Commit changes**.
5. انتظر ثوانٍ حتى ترفع الملفات كلها — تأكد أنك تشوف كل الملفات ظاهرة في قائمة المستودع.

**أو عبر سطر الأوامر (إذا كنت مرتاح معه):**

```bash
cd مسار-مجلد-الموقع-على-جهازك
git init
git add .
git commit -m "Initial site"
git branch -M main
git remote add origin https://github.com/USERNAME/ahmed-visual-site.git
git push -u origin main
```

استبدل `USERNAME` باسم مستخدمك في GitHub، و`ahmed-visual-site` باسم المستودع الذي أنشأته.

---

## الجزء الثالث: النشر على Netlify

### 1. اربط Netlify بحساب GitHub

1. روح إلى [app.netlify.com](https://app.netlify.com) وسجّل دخول (أو أنشئ حساب — الأسهل تسجّل دخول مباشرة بحساب GitHub نفسه، زر **Sign up with GitHub**).
2. من لوحة Netlify الرئيسية، اضغط **Add new site → Import an existing project**.
3. اضغط زر **Deploy with GitHub**.
4. أول مرة، Netlify يطلب صلاحية الوصول لحسابك في GitHub — اضغط **Authorize Netlify**.
5. يظهر لك خيار: إما تعطي Netlify صلاحية على **كل** مستودعاتك، أو فقط مستودع محدد. الأفضل والأكثر أمانًا: اختر **Only select repositories** وحدد `ahmed-visual-site` فقط، ثم اضغط **Install**.

### 2. اختر المستودع واضبط الإعدادات

1. من قائمة المستودعات التي تظهر، اضغط على `ahmed-visual-site`.
2. في صفحة **Site settings**:
   - **Branch to deploy:** اتركه `main`.
   - **Build command:** اتركه فاضي تمامًا (الموقع ملفات ثابتة، ما يحتاج بناء).
   - **Publish directory:** اتركه فاضي أو ضع نقطة `.` — ملف `netlify.toml` المرفق يضبط هذا تلقائيًا.
3. اضغط **Deploy ahmed-visual-site** (أو **Deploy site**).

### 3. تحقق من النشر الأول

1. انتظر أقل من دقيقة — Netlify يبني وينشر الموقع.
2. بمجرد ما تشوف علامة ✅ **Published**، بيظهر لك رابط مؤقت شكله مثل:
   `https://random-name-123456.netlify.app`
3. افتح هذا الرابط وتأكد:
   - الموقع العام يفتح ويعرض المحتوى (البيانات الافتراضية من Supabase).
   - `/admin.html` يفتح شاشة تسجيل الدخول.

إذا الموقع ما يعرض محتوى (صفحة فاضية أو خطأ)، ارجع تأكد إن `supabase-config.js` فيه القيم الصحيحة اللي نسختها في الجزء الأول، وإنك رفعته ضمن الملفات على GitHub.

### 4. اربط دومين `ahmedvis.com`

1. من صفحة الموقع في Netlify، روح إلى **Domain settings** (أو **Domain management**).
2. اضغط **Add a domain**.
3. اكتب `ahmedvis.com` واضغط **Verify**، ثم **Add domain**.
4. Netlify يعرض عليك طريقتين لربط الدومين — اختر واحدة:

   **الطريقة أ — الأسهل والموصى بها: تغيير Nameservers**
   - روح لحسابك عند مزوّد الدومين (GoDaddy, Namecheap, أو غيره).
   - غيّر Nameservers للدومين إلى الأربعة عناوين اللي يعطيك إياها Netlify بالضبط (تشبه `dns1.p01.nsone.net` وهكذا).
   - بهذا Netlify يتحكم بكل إعدادات DNS للدومين تلقائيًا (بما فيها `www` وشهادة SSL).

   **الطريقة ب — إذا تفضل إبقاء DNS عند مزوّد الدومين:**
   - أضف سجل **A** يشير إلى عنوان IP الذي يعطيك إياه Netlify (يظهر في نفس الصفحة).
   - أضف سجل **CNAME** لـ `www` يشير إلى الرابط المؤقت `random-name-123456.netlify.app`.

5. بعد الحفظ، انتظر من عدة دقائق إلى بضع ساعات (نادرًا حتى 24 ساعة) حتى تنتشر تغييرات DNS حول العالم.
6. بمجرد التحقق، Netlify يصدر شهادة HTTPS (SSL) مجانية للدومين تلقائيًا — لا حاجة لأي خطوة إضافية منك.
7. للتأكد: افتح `https://ahmedvis.com` بعد فترة — لازم يفتح نفس الموقع مباشرة بقفل أمان 🔒 في المتصفح.

### 5. أي تحديث كود مستقبلي

أي مرة تعدّل على ملفات الكود نفسها (نادر — لأن المحتوى يتغيّر من لوحة التحكم لا من الكود) وترفعه على GitHub (commit/push أو رفع ملف جديد من الموقع)، Netlify ينشره تلقائيًا خلال ثوانٍ بدون أي خطوة يدوية إضافية.

---

## الجزء الرابع: تفعيل نموذج التواصل (رد تلقائي فوري عبر EmailJS)

نموذج "Start a project" في الموقع يرسل الرسائل عبر خدمة **EmailJS** المجانية — تستقبل رسالة الزائر، توصّلك بريديًا عبر حساب بريدك الشخصي (Gmail مثلاً)، **وترسل للزائر تلقائيًا رسالة تأكيد فورية** توعده بالرد خلال 48 ساعة. الإعداد يصير مرة واحدة فقط، ويحتاج خطوات أكثر شوي من البديل السابق لأنه يعطيك تحكم أكبر بمحتوى الرسائل.

### 1. أنشئ حساب EmailJS واربط بريدك

1. روح إلى [emailjs.com](https://www.emailjs.com) وأنشئ حساب مجاني.
2. من القائمة الجانبية، روح إلى **Email Services** واضغط **Add New Service**.
3. اختر **Gmail** (أو مزوّد بريدك الفعلي إذا مختلف) واربط حسابك `hello@ahmedvis.com` — بيطلب منك تسجيل دخول والموافقة على الصلاحيات.
4. بعد الربط، انسخ **Service ID** الظاهر (شكله مثل `service_xxxxxxx`) — بتحتاجه بعدين.

### 2. أنشئ قالب إشعارك أنت (Template 1)

هذا القالب هو الرسالة اللي توصلك أنت فيها تفاصيل الاستفسار.

1. من القائمة الجانبية، روح إلى **Email Templates** واضغط **Create New Template**.
2. في حقل **To Email**، اكتب `{{to_email}}`.
3. في **Subject**، اكتب مثلاً: `New project inquiry from {{from_name}}`.
4. في محتوى الرسالة (Content)، اكتب:
   ```
   Name: {{from_name}}
   Email: {{from_email}}
   Project type: {{project_type}}

   Message:
   {{message}}
   ```
5. احفظ، وانسخ **Template ID** الظاهر أعلى الصفحة (شكله مثل `template_xxxxxxx`).

### 3. أنشئ قالب الرد التلقائي للزائر (Template 2)

هذا قالب منفصل تمامًا — الرسالة اللي توصل للزائر نفسه.

1. اضغط **Create New Template** مرة ثانية (قالب جديد).
2. في حقل **To Email**، اكتب `{{from_email}}` (بريد الزائر نفسه، مو بريدك).
3. في **Subject**، اكتب: `Thanks for reaching out — Ahmed Visual`.
4. في المحتوى، اكتب رسالتك الاحترافية، مثلاً:
   ```
   Hi {{from_name}},

   Thanks for getting in touch about your project. I've received
   your message and will get back to you within 48 hours.

   In the meantime, feel free to check out more of my work at
   ahmedvis.com.

   Best,
   Ahmed
   Ahmed Visual
   ```
5. احفظ، وانسخ **Template ID** الخاص بهذا القالب (مختلف عن الأول).

### 4. انسخ المفتاح العام (Public Key)

1. من القائمة الجانبية، روح إلى **Account → General**.
2. انسخ **Public Key** الظاهر هناك.

### 5. ضع كل القيم في كود الموقع

1. افتح ملف `supabase-config.js`.
2. ابحث عن الأسطر الأربعة بالأسفل واستبدل كل قيمة بالقيمة الحقيقية اللي نسختها:
   ```js
   const EMAILJS_PUBLIC_KEY = 'YOUR-EMAILJS-PUBLIC-KEY';
   const EMAILJS_SERVICE_ID = 'YOUR-EMAILJS-SERVICE-ID';
   const EMAILJS_TEMPLATE_NOTIFY = 'YOUR-NOTIFY-TEMPLATE-ID';
   const EMAILJS_TEMPLATE_AUTOREPLY = 'YOUR-AUTOREPLY-TEMPLATE-ID';
   ```
   - `EMAILJS_PUBLIC_KEY` ← من خطوة 4
   - `EMAILJS_SERVICE_ID` ← من خطوة 1
   - `EMAILJS_TEMPLATE_NOTIFY` ← من خطوة 2 (القالب اللي يوصلك أنت)
   - `EMAILJS_TEMPLATE_AUTOREPLY` ← من خطوة 3 (قالب الرد على الزائر)
3. احفظ الملف، وارفعه على GitHub (استبدال) — Netlify ينشره تلقائيًا خلال ثوانٍ كالعادة.

### 6. جرّب النموذج

1. افتح `ahmedvis.com`، روح لقسم **Start a project**، وعبّي النموذج ببريدك الشخصي للتجربة.
2. اضغط **Send message**.
3. لازم توصلك رسالتين: واحدة على `hello@ahmedvis.com` فيها تفاصيل الاستفسار، وواحدة على البريد اللي كتبته في النموذج (رسالة التأكيد التلقائية).

> **ملاحظة:** الخطة المجانية في EmailJS تكفي حتى 200 رسالة شهريًا (كل رسالة تحسب مرتين لأنها قالبين — يعني عمليًا حوالي 100 استفسار شهريًا) — تمامًا يناسب حجم استخدامك المتوقع.

---

## الجزء الخامس: تحديث قاعدة البيانات (مرة واحدة فقط)

هذا القسم ضروري فقط لو سبق وشغّلت `supabase-schema.sql` على مشروعك قبل هذا التحديث (يعني عندك بيانات موجودة بالفعل في Supabase). يطبّق ثلاث تعديلات بدون ما يمسح أي محتوى سابق:

- يحذف حقل الإيميل العام والسيرة الذاتية غير المستخدمين
- يضيف حقل LinkedIn
- ينشئ بنية أداة "مشاركة الملفات" الجديدة

### الخطوات

> إذا ما شغّلت ملف `supabase-migration-1-logo.sql` من التحديث السابق (الشعار)، شغّله أولًا بنفس الطريقة، ثم انتقل للملف التالي.

1. افتح ملف `supabase-migration-2-privacy-and-files.sql` (في هذا المجلد) وانسخ كل محتواه.
2. روح لمشروعك في Supabase → **SQL Editor** → **New query**.
3. الصق المحتوى واضغط **Run**.
4. تأكد إنه ما طلع خطأ أحمر — "Success" يعني كل شي تمام.

> إذا هذا أول مرة تعدّ فيها المشروع من الصفر (ما شغّلت `supabase-schema.sql` القديم من قبل)، تجاهل هذا الجزء تمامًا — ملف `supabase-schema.sql` المرفق أصلاً محدّث وفيه كل التعديلات الجديدة.

---

## الجزء السادس: خصوصية الإيميل، LinkedIn، ومشاركة الملفات

### الإيميل لم يعد ظاهرًا للزوّار

بطاقة "Email" اللي كانت تظهر في قسم التواصل بالموقع العام تم حذفها بالكامل. الإيميل الآن يُستخدم فقط **داخليًا** — لتوصيل رسائل نموذج التواصل عبر EmailJS (كما شرحنا في الجزء الرابع) — ولا يظهر لأي زائر في أي مكان بالموقع.

### إضافة رابط LinkedIn

1. افتح `ahmedvis.com/admin.html` وسجّل دخول.
2. من قسم **Site**، ابحث عن حقل **LinkedIn URL**.
3. الصق رابط ملفك الشخصي الكامل (مثل `https://linkedin.com/in/ahmed-example`).
4. يُحفظ تلقائيًا ويظهر فورًا في بطاقة LinkedIn بقسم التواصل بالموقع.

### أداة مشاركة الملفات (خاصة بك فقط — غير ظاهرة في الموقع)

قسم جديد في لوحة التحكم اسمه **"File sharing"** يتيح لك رفع أي ملف (PDF، صورة، أي نوع) وأخذ رابط مباشر له، لإرساله لأي عميل عبر واتساب أو أي وسيلة تحب — بديل بسيط عن Google Drive أو Dropbox، ومدمج مباشرة في لوحة تحكمك.

**كيف تستخدمها:**

1. من القائمة الجانبية في لوحة التحكم، اضغط **File sharing**.
2. اضغط **Upload a file** واختر الملف من جهازك.
3. بعد الرفع، بيظهر الملف في القائمة مع اسمه وتاريخ الرفع.
4. اضغط **Copy link** — الرابط ينسخ تلقائيًا لجهازك، جاهز للصقه بأي مكان.
5. لحذف ملف قديم ما تحتاجه: اضغط ✕ بجانبه وأكّد.

> **مهم:** هذه الأداة **غير مرتبطة بالموقع العام إطلاقًا** — لا تظهر فيها أي قائمة أو صفحة لأي زائر. فقط أنت (بعد تسجيل الدخول) تقدر ترى وتدير الملفات المرفوعة. أي شخص عنده الرابط المباشر يقدر يفتح الملف، لكن محد يقدر يتصفح أو يشوف باقي ملفاتك.

---

## الجزء السابع: استخدام لوحة التحكم

1. افتح `ahmedvis.com/admin.html`.
2. سجّل دخول بالبريد وكلمة المرور اللي أنشأتهم في Supabase (الجزء الأول، خطوة 3).
3. من القائمة الجانبية، عدّل أي قسم: **Site** (البيانات الأساسية)، **Hero** (العنوان الرئيسي)، **About** (نبذة عنك)، **Contact**، **Projects** (المشاريع)، أو **File sharing** (مشاركة الملفات الخاصة).
4. أي تعديل نص يُحفظ تلقائيًا بعد نصف ثانية من توقفك عن الكتابة (تشوف "Saving…" ثم "Saved" أعلى الصفحة).
5. لرفع صورة أو شعار: اضغط زر **Upload** بجانب الحقل المناسب واختر الملف — يُرفع مباشرة ويظهر في الموقع فورًا.
6. لإضافة مشروع جديد: من قسم **Projects**، اضغط **+ Add project**، عبّئ التفاصيل، وارفع صورة.
7. لإعادة ترتيب المشاريع: استخدم الأسهم ↑ ↓ بجانب كل مشروع.
8. لحذف مشروع: اضغط ✕ وأكّد.

كل هذا يظهر مباشرة لأي زائر يفتح الموقع — بدون أي خطوة نشر إضافية. الاستثناء الوحيد هو قسم **File sharing**، اللي يبقى خاص بك فقط ولا يظهر في الموقع العام أبدًا.

---

## أسئلة شائعة

**هل لوحة التحكم آمنة؟**
نعم. التسجيل مقفول على حسابك فقط (لا يوجد تسجيل عام)، والحفظ محمي بقواعد صلاحيات (RLS) داخل قاعدة البيانات نفسها — حتى لو عرف أحد رابط الـ API، ما يقدر يعدّل أي شيء بدون تسجيل دخول صحيح.

**هل أحتاج أدفع أي شيء؟**
الخطط المجانية في GitHub وNetlify وSupabase تكفي بسهولة لموقع بورتفوليو شخصي. الشيء الوحيد المدفوع هو الدومين نفسه (`ahmedvis.com`، وهو موجود لديك أصلًا).

**نسيت كلمة مرور لوحة التحكم؟**
من Supabase → Authentication → Users، اختر المستخدم وأرسل رابط إعادة تعيين، أو غيّرها مباشرة من هناك.

**أبي أضيف حساب لشخص ثاني يساعدني بإدارة الموقع؟**
من نفس صفحة Authentication → Users في Supabase، اضغط Add user وأنشئ له حسابًا بنفس الطريقة.
